import { BasicRepository } from "@commit-tech/system-shared";
import type { WhatsappEntity, WhatsappModel } from "../entities/whatsapp.entity";


export abstract class WhatsappRepository extends BasicRepository<WhatsappModel, WhatsappEntity> {
 
}